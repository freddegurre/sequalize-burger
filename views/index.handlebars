<div class="container">
    <div class="center">
      <h1>Burgers</h1>
    </div>
  </div>

  <div class="row">
    <div class="col-md-6">
      <div class="center">
        {{#each burgers}}
        <div class="row burgerDiv">
          <span>{{id}}. {{burger_name}}</span>
          <button class="devoure btn-warning pull-right" data-id="{{id}}" data-devoured="{{devoured}}">
            Devoure!
          </button>
        </div>
        {{/each}}
      </div>
    </div>

    <div class="col-md-6">
      <div class="center">

        {{#each eaten}}
        <div class="devoured">
          <span>{{id}}.{{burger_name}} </span>
        </div>
        {{/each}}

      </div>
    </div>
  </div>
  <div class="row">
    <div class="center">
      <form class="create-form">
        <div class="form-group">
          <textarea id="burger" name="newBurg" rows="4" cols="40"></textarea>
        </div>
        <button class="btn-primary" type="submit" id="buttonPost">Order burger</button>
      </form>
    </div>
  </div>

</div>